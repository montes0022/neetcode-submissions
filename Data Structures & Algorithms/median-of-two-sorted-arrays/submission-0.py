class Solution:
    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:
        l = 0
        r = len(nums1) - 1
        
        l2 = 0
        r2 = len(nums2) - 1

        res - 0.0

        smallest = min(len(nums1), len(nums2))


        return res
